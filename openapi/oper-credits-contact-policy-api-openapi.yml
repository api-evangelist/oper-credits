openapi: 3.2.0
info:
  title: Oper Credits Contact Policy API
  version: ''
tags:
- name: Contact Policy
paths:
  /api/contact-policy/:
    get:
      operationId: api_contact_policy_retrieve
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContactPolicy'
          description: ''
      security:
      - jwtAuth: []
      tags:
      - Contact Policy
      summary: Api contact policy retrieve
      x-summary-source: derived
components:
  schemas:
    Language:
      description: Can be managed at /resources/language
      properties:
        definition:
          type: string
        id:
          type: integer
        order:
          readOnly: true
          type: integer
      required:
      - definition
      - id
      - order
      type: object
    ContactPolicy:
      properties:
        configuration_id:
          format: uuid
          type: string
        content:
          type: string
        language:
          $ref: '#/components/schemas/Language'
        tenant:
          type:
          - integer
          - 'null'
        title:
          maxLength: 128
          type: string
      required:
      - content
      - language
      - title
      type: object
  securitySchemes:
    jwtAuth:
      bearerFormat: JWT
      scheme: bearer
      type: http
